# Editor / OS
.DS_Store
*.swp
*~

# RuFlo / Claude Code workspace artifacts (per-developer, not shipped with plugin)
.claude/
.claude-flow/
.mcp.json
CLAUDE.md

# Composer / PHPUnit
/vendor/
composer.lock
.phpunit.result.cache
.phpunit.cache/
